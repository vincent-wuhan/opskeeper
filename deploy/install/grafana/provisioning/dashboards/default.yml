apiVersion: 1

providers:
  - name: opskeeper-default
    orgId: 1
    folder: opskeeper
    folderUid: opskeeper
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards/json

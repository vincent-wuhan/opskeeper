apiVersion: 1

datasources:
  - name: opskeeper-prometheus
    uid: opskeeper-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090/prometheus
    isDefault: true
    editable: false
